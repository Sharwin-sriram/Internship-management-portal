'use client';

/**
 * RegistrationForm — legacy component kept for compatibility.
 * The main registration UI lives in app/register/page.tsx.
 * This re-exports the same form logic for any places that still import it.
 */
export { default } from '../app/register/page';
